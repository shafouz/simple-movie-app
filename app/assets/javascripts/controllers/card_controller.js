import { Controller } from "stimulus"

export default class extends Controller {
  /*
  initialize() {
    let html = this.element.innerHTML
    //console.log(html)
    if (html.length > 200) { 
      console.log("aspjdpojdas")
      this.element.innerHTML = "COOLIO"
    }
  }
  */
}
