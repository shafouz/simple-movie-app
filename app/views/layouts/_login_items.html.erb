<% if user_signed_in? %>
  <li>
  <%= button_to('Logout', destroy_user_session_path, class: "cursor-pointer bg-transparent", method: :delete) %>        
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path) %>  
  </li>
<% end %>
